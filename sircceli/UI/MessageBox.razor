@namespace sircceli.UI
@using RazorConsole.Components
@using Spectre.Console
@using Markup = RazorConsole.Components.Markup
@using Panel = RazorConsole.Components.Panel
@using Rows = RazorConsole.Components.Rows
<Panel Border="BoxBorder.Heavy" Expand="true">
    <Rows>
        @{
            string msg1 = "13:37 | <user1> | Hello world!";
            string msg2 = "13:37 | <user2> | This is a message with a very long content that should be truncated when it exceeds the width of the panel.";
            string msg3 = "13:37 | <user3> | Bye world!";
        }
        <Markup Content="@msg1"/>
        <Markup Content="@msg2"/>
        <Markup Content="@msg3"/>
    </Rows>
</Panel>